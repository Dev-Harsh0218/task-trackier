# task-trackier
Task-Trackier backend — Django API + Kafka activity-log consumer + Redis-backed SSE gateway. Used by 100-200 employees at AppSuccessor for sprints, backlog, Kanban, and role-based workflows.
